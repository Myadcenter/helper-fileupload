# Myadcenter.scraper file upload test helper  

Tool to emulate file upload function. Can be used as a helper for 
tests of myadcenter.scraper package.

To use in tests, run 
```
node ./app.js
```
Then, run tests in myadcenter.scraper:
```
cd myadcenter.scraper
npm test
```